{
  "name": "tickets-types",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/tickets/types/src",
  "prefix": "zoo",
  "tags": ["domain:tickets", "type:types"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/tickets/types"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
