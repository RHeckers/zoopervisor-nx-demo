{
  "name": "enclosures-types",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/enclosures/types/src",
  "prefix": "zoo",
  "tags": ["domain:enclosures", "type:types"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/enclosures/types"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
