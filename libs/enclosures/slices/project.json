{
  "name": "enclosures-slices",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/enclosures/slices/src",
  "prefix": "zoo",
  "tags": ["domain:enclosures", "type:slice"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/enclosures/slices"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
