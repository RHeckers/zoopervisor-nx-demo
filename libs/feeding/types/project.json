{
  "name": "feeding-types",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/feeding/types/src",
  "prefix": "zoo",
  "tags": ["domain:feeding", "type:types"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/feeding/types"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
