{
  "name": "animals-types",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/animals/types/src",
  "prefix": "zoo",
  "tags": ["domain:animals", "type:types"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/animals/types"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
