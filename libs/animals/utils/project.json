{
  "name": "animals-utils",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/animals/utils/src",
  "prefix": "zoo",
  "tags": ["domain:animals", "type:util"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/animals/utils"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
