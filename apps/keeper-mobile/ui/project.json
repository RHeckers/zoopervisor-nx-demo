{
  "name": "keeper-mobile-ui",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "apps/keeper-mobile/ui/src",
  "prefix": "zoo",
  "tags": ["app:keeper-mobile", "type:ui"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/apps/keeper-mobile/ui"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
