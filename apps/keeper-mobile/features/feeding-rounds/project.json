{
  "name": "keeper-mobile-feeding-rounds",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "apps/keeper-mobile/features/feeding-rounds/src",
  "prefix": "zoo",
  "tags": ["app:keeper-mobile", "type:feature"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/apps/keeper-mobile/features/feeding-rounds"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
