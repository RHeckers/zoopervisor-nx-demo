import { Routes } from '@angular/router';
import { AnimalDetailComponent } from './animal-detail.component';

export const animalDetailRoutes: Routes = [
  { path: '', component: AnimalDetailComponent },
];
