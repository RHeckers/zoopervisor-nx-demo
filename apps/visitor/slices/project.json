{
  "name": "visitor-slices",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "apps/visitor/slices/src",
  "prefix": "zoo",
  "tags": ["app:visitor", "type:slice"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/apps/visitor/slices"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
