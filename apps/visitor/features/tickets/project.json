{
  "name": "visitor-tickets",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "apps/visitor/features/tickets/src",
  "prefix": "zoo",
  "tags": ["app:visitor", "type:feature"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/apps/visitor/features/tickets"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
